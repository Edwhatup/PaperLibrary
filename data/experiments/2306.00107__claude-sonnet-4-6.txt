音乐信息检索领域长期面临一个根本性的困境：要让机器真正理解音乐，需要大量带标注的音频数据，但音乐数据的标注成本极高，而且各国版权法的限制让数据共享变得异常困难。与此同时，自然语言处理和计算机视觉领域已经用自监督学习的方式漂亮地绕开了这个问题——用海量无标注数据预训练一个通用模型，再迁移到各种下游任务。那么，这套思路能不能搬到音乐理解上来？这正是这篇提出MERT模型的论文试图回答的核心问题。

要理解这篇论文的动机，先要搞清楚已有工作的两个主要痛点。第一个痛点是任务碎片化。传统的音乐信息检索，英文缩写MIR，涵盖了非常广泛的任务：音乐标签分类、节拍追踪、调性检测、乐器识别、情感回归、人声技巧识别、歌手识别、音源分离等等，每一项任务以前都需要单独设计特征和模型，无法复用。第二个痛点是现有预训练模型要么不开源，要么太重。当时最接近通用音乐预训练模型的是JukeMIR，它建立在Jukebox这个生成模型上，Jukebox用了数十亿参数的层级自回归Transformer解码器。问题有多严重呢？论文里提到，就算用一张消费级的3090显卡，在MTG数据集上做推理都要好几个星期。这完全不是一个面向研究社区和工业界可以实际使用的解决方案。

在这个背景下，MERT的目标就很清晰了：设计一个参数量合理、完全开源、能同时应对多种MIR任务的音乐音频预训练模型，而且不依赖人工标注数据。

MERT的基础框架借鉴自语音领域的HuBERT。HuBERT的核心思想是掩码语言建模，也就是MLM。具体做法是：把音频序列的一部分随机遮掉，然后让模型去预测被遮掉部分对应的离散标签，这些标签不是人工标注的，而是由一个"教师模型"在预训练之前或者预训练过程中生成的伪标签。模型学会准确预测这些伪标签的过程，实际上就是在学习音频的深层表示。

HuBERT在语音上用的教师是对MFCC特征做K-means聚类，MFCC是一种捕捉语音音色和共振峰的传统特征。这个方案在语音上很好用，因为语音的音高变化相对简单，主要信息集中在音色和节奏模式上。但音乐有一个根本性的不同：音乐中同时有多个音高，有复杂的和声结构，有精确的调性关系，而MFCC根本就不是为捕捉这些特性设计的。论文的消融实验也印证了这一点，直接把HuBERT的框架搬到音乐数据上，调性检测任务的表现非常糟糕，模型几乎没有学到任何音高和调性信息。

这就是MERT最核心的设计创新所在：引入两类教师模型，一个负责声学信息，一个负责音乐性信息。

先说声学教师。论文最终选定的是EnCodec，这是Meta发布的一个高保真神经音频编解码器，它的核心结构是残差向量量化变分自编码器，简称RVQ-VAE。可以把EnCodec理解成一个极其高效的音频压缩器：它把24kHz的音频波形编码成8层残差离散码本，每层是一个索引序列，帧率是75Hz。这8层码本是残差叠加的关系，第一层捕捉最主要的声学特征，后续每一层捕捉上一层没有覆盖到的残差细节。从最底层到最顶层，涵盖了从宏观音色到微观细节的完整声学信息。EnCodec的一个关键优势是它可以从这些离散码重建出高质量的波形，这意味着这些离散表示里确实压缩进了非常丰富的声学信息。

为什么不继续用K-means呢？论文里提到，K-means方案有两个硬伤：一是很难扩展到大规模数据集，因为K-means的计算复杂度随着类别数量线性增长，而音乐帧比语音帧信息量更大，需要更多的聚类中心，这个计算负担会迅速变得不可接受；二是K-means对初始化非常敏感，结果不稳定。EnCodec作为一个已经预训练好的神经网络编码器，直接用就行，没有这些问题，而且它提供的声学信息质量也更高。

再说音乐教师，这是MERT区别于所有已有语音预训练模型的关键。论文引入了CQT，也就是常数Q变换谱图，作为音乐性的教师信号。CQT是一种特殊的频率变换，它和傅里叶变换不同的地方在于：傅里叶变换的每个频率箱宽度是相等的，而CQT的箱宽和频率成正比，每个八度包含相同数量的频率箱。这个设计有什么好处呢？打个比方，钢琴上的中央C是261赫兹，它的高八度是523赫兹，再高一个八度是1046赫兹。虽然频率在翻倍，但这三个音在音乐上的关系完全相同，它们只是音高不同但功能等价。CQT的设计让这种八度等价关系在表示中天然体现出来，因此CQT谱图对音高检测、和弦识别、调性分析这类任务有天然的归纳偏置。

MERT使用均方误差损失来重建被遮掩输入对应的CQT谱图，而不是像对声学教师那样做离散分类预测。最终的总损失是声学预测损失和CQT重建损失的线性组合，通过一个权重超参数来平衡两者的贡献。论文通过超参数搜索发现，最优的CQT损失权重是0.1，也就是声学损失还是主角，音乐性损失作为辅助正则项。这个比例意味着模型的主要学习信号来自声学离散预测，但CQT损失在训练中扮演了一个非常有价值的引导角色，把模型的注意力引向音高和调性维度。

来看一个具体数字来感受这个设计的效果。在消融实验中，只用MFCC做K-means教师时，调性检测任务的精确度大约只有十几个百分点的量级。当给K-means加入Chroma特征作为额外输入后，精确度立刻跳升到了更高的水平。而进一步引入显式的CQT教师后，不仅调性检测任务继续提升，情感回归和流派分类这些需要更全局信息的任务也同步受益，说明音高信息的引入让模型学到了更全面的音乐语义表示。

训练过程中还有一个被论文专门分析的重要问题：训练不稳定性。当模型规模从95M参数扩展到330M时，模型训练表现出了比NLP或图像领域同等规模模型更高的不稳定倾向，表现为梯度持续被裁剪，以及损失出现剧烈的尖峰跳变，有时甚至直接导致训练崩溃。论文尝试了几种常见的补救方案，比如从之前的检查点恢复训练、降低学习率、使用DeepNorm这种专门为深层Transformer设计的归一化方案，但都没有解决问题，有些方案甚至让性能变得更差。

最终有效的解决方案有两个。第一是注意力放松技术，来自WavLM的设计，它对注意力计算做了一些修改，让梯度流动更平稳。第二是把后置层归一化（Post-LN）改为前置层归一化（Pre-LN）。在标准Transformer中，Post-LN是在残差连接之后做归一化，而Pre-LN是在子层之前就做归一化。Pre-LN在训练早期的梯度更稳定，对于大规模模型来说这个差异相当显著。这两个改动组合起来，才让330M参数的模型训练得以稳定进行。这个发现对未来做音频领域大模型预训练的研究者有直接的参考价值。

关于数据，MERT从互联网上收集了16万小时的音乐录音作为大规模预训练语料。95M参数的基础版模型使用其中1000小时的子集训练，330M参数的大模型使用全量数据。论文还专门提供了一个完全使用公开数据集训练的版本，叫做MERT-95M-public，用的是Music4All数据集，约910小时，主要是流行音乐，风格多样性相对有限。

训练时还有一个值得关注的工程决策：音频片段统一截断为5秒。这背后的考量是，在有限的计算资源下，自注意力机制的计算复杂度随序列长度平方增长，更长的序列意味着更小的批次大小。论文发现，增大批次大小对性能的提升比增加上下文长度更显著，所以选择了用更大批次的短片段来训练。5秒大致对应音乐中的两个小节，是一个有实际音乐结构意义的长度。为了让模型在推理时能处理更长的音频，MERT使用了卷积相对位置编码，这样即使推理时输入远长于5秒，模型也能泛化。

在测试协议上，论文坚持使用探针评估方式，也就是冻结预训练模型的参数，只训练一个轻量级的MLP头部来完成下游任务，不做全模型微调。这种方式更能反映预训练表示本身的质量，因为如果允许全量微调，性能差异很大程度上会被下游任务数据量和微调超参数所掩盖。

评估覆盖了14个音乐理解任务，可以分成两类。帧级别任务包括：MagnaTagATune和MTG-Jamendo上的音乐标签、Giantsteps数据集上的调性检测、GTZAN和MTG-Genre上的流派分类、Emomusic上的情感回归、NSynth和MTG-instrument上的乐器分类、NSynth上的音高分类、VocalSet上的人声技巧检测和歌手识别。序列级别任务包括：GTZAN Rhythm上的节拍追踪和MUSDB18上的音源分离。这14个任务的覆盖范围非常广，从底层感知任务（音高分类、节拍追踪）到中层语义任务（调性检测、乐器识别）再到高层语义任务（流派分类、情感回归）都有，这个评估体系本身就是论文的一个贡献。

从实验结果来看，MERT-330M在综合平均分上达到了与10个不同前沿模型（包括有监督方法）各自最优结果的组合相当的水平，并在4个指标上成为新的最优。更值得关注的是95M参数的基础版本，其性能和330M版本在大多数任务上相差不超过0.1个百分点，说明MERT的预训练范式非常高效，不需要堆砌极大的模型规模也能达到很好的效果。

相比之下，Jukebox有50亿参数，而MERT-95M只有其约2%的规模，MERT-330M也只有Jukebox的7%，却能在探针评估下取得更好或相当的结果。这个效率差异在实际使用中意义极大——MERT可以在普通GPU上运行推理，而不是需要等几个星期。

从任务分布来看，MERT在局部信息类任务上表现特别突出，比如节拍追踪、音高分类、歌手识别。这和5秒训练上下文的设计是一致的，因为节拍和音高这类信息在短时窗口内就能充分体现。在需要全局信息的任务上，比如音乐标签和调性检测，MERT的表现也接近最优水平，论文把这归功于相对位置编码和Transformer自注意力机制本身对全局依赖关系的建模能力。

论文的局限性也说得很诚实。最主要的限制就是5秒的训练上下文，对于需要理解更长结构的任务来说确实是个天花板，比如理解一首歌完整的曲式结构、副歌与主歌之间的对比关系、整体的调性布局等等，这些在5秒窗口内都很难捕捉。论文提到，他们计划在获得更多计算资源后，在更长的上下文上继续训练。另一个局限是即便采取了一系列稳定化措施，在较大批次和较大模型规模下的半精度训练中仍然会遇到梯度爆炸问题。在扩展到330M时还观察到了逆缩放效应，就是某些任务上更大的模型反而表现更差，说明训练稳定性还有提升空间。

对于做音频、语音或音乐理解方向研究的人，这篇论文有几点非常具体的借鉴价值。第一，双教师设计的思路本身是可迁移的：在任何领域，如果你能识别出领域特有的结构性知识（比如音乐里的音高关系），把这个知识编码成一个额外的辅助重建目标，往往能显著提升预训练模型对该领域特性的敏感度。第二，RVQ-VAE作为声学教师的方案已经被证明是一个优秀的通用基础，比K-means更容易扩展，EnCodec现在也被广泛用于其他音频语言模型。第三，Pre-LN加注意力放松这个组合对音频领域大模型训练稳定性的作用是经过系统验证的，在设计自己的音频预训练框架时应该优先考虑这个配置，而不是默认沿用NLP里Post-LN的惯例。第四，批次大小与上下文长度之间的权衡在音频领域格外突出，在资源有限时优先保证批次大小而不是上下文长度，是一个有实验依据的经验法则。

MERT说明了一件重要的事：音乐音频的自监督预训练不能简单地把语音领域的方案照搬过来，因为音乐的音调性和谐波结构对表示学习有独特的要求，必须在教师设计层面就把这个领域知识注入进去，而做到这一点并不需要极其庞大的模型规模，关键在于预训练信号的质量和多样性。